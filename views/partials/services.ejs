<div class="row mb-3 bg-primary text-light">
  <section class="col-12" id="ourWork">
    <div class="text-center mt-2 mb-5">
      <h1><%= headings[0].servicesHeading %></h1>
    </div>
    <ul class="col-lg-3 col-12">
      <h3>IT</h3>
      <% for (service of services){ %>
        <li><%= service.description %></li>
        <%}%>
    </ul>
  </section>
</div>
